# Dino Game - Unity

The dino game built in Unity with the [LEADR Unity SDK](https://github.com/LEADR-official/leadr-sdk-unity).

## Next steps

In the editor
 1. Add LeadrIntegration component to the GameManager GameObject
 2. Wire up its settings, leaderboardPanel, scoreListContainer, and scoreEntryPrefab fields
 3. Assign the LeadrIntegration reference on the GameManager's leadr field
 4. Remove the now-missing CanvasExampleDemo component (will show as "Missing Script")
 5. Remove or hide the leaderboard toggle button from the scene
